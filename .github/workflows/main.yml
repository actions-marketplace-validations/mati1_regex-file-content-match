on: [push]

jobs:
  hello_world_job:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3
      - name: Count New Lines in JavaScript
        uses: ./
        with:
          file_name_regex: "^(.*)\\.js$"
          file_content_regex: "\\n"
