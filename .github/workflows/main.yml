on: [push]

jobs:
  hello_world_job:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3
      - name: Count New Lines in JavaScript
        uses: ./
        with:
          fileNameRegex: '^(.*)\.js$'
          fileContentRegex: '\n'
